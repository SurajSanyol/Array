class Solution {
  public:
    int largest(vector<int> &arr) {
        // code here
        int maxNum=arr[0];
        
        for(int i=1;i<arr.size();i++){
            if(arr[i]>maxNum){
                maxNum= arr[i];
            }
        }
        return maxNum;
    }
};
